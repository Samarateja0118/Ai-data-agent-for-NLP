# Only needed for local dev, where the Vite dev server (5173) and the API
# (uvicorn, 8000) run as separate processes on different origins. Leave unset
# in production/Vercel — frontend and API share one origin there, so the
# client falls back to relative /api/* requests.
VITE_API_BASE_URL=http://localhost:8000
